## IDF Component Manager Manifest File
dependencies:
  idf:
    version: '>=6.0'
  # esp-mqtt is no longer bundled with IDF 6.x - it lives in the registry.
  # Replaces upstream ESPAltherma's PubSubClient.
  espressif/mqtt: '*'
